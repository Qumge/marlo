.venv/
__pycache__/
*.pyc
.pytest_cache/
*.egg-info/
build/
dist/
.coverage

# actool output, rebuilt by packaging/make_asset_catalog.py on every macOS build
surfaces/gui/src-tauri/gen/Assets.car
surfaces/gui/src-tauri/gen/.iconset-build/

# Local secrets (live-smoke BYO keys) — never committed
.env

# Worktrees（EnterWorktree 建在这儿）和 SDD 的临时工作区 —— 都是本地脚手架
.claude/worktrees/
.superpowers/
